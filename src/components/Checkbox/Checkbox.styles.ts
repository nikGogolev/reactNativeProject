import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  box: {
    height: 30,
    width: 30,
    borderRadius: 15,
    borderWidth: 2,
    borderColor: 'black',
  },
  filled: {
    backgroundColor: 'black',
  },
});
